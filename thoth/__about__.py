"""Version and package metadata for Thoth.

This module is used by the build system and at runtime to expose
the current package version. Do not rely on it for feature detection;
use __version__ for display or logging only.
"""

__version__ = "1.0.0"
